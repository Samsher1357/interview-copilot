# Interview Copilot - Environment Configuration
# This single .env.local file is used by BOTH frontend and backend

# =============================================================================
# FRONTEND CONFIGURATION
# =============================================================================

# Backend API URL
# Development: http://localhost:3001
# Production: Your deployed backend URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# UI Configuration
NEXT_PUBLIC_AUTO_SPEAK=false


# =============================================================================
# BACKEND CONFIGURATION
# =============================================================================

# Server Configuration
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# OpenAI Configuration
# Get your key at: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here
# Available models: gpt-3.5-turbo, gpt-4o-mini, gpt-4, gpt-4o

# Google Gemini Configuration (Optional - FREE!)
# Get your key at: https://makersuite.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here
# Available models: gemini-1.5-flash, gemini-1.5-pro, gemini-2.0-flash-exp

# Deepgram Configuration (Required for speech recognition)
# Get your key at: https://console.deepgram.com
DEEPGRAM_API_KEY=your_deepgram_api_key_here

# AI Response Configuration
AI_MAX_TOKENS=1200


# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================

# 1. Copy this file to .env.local in the ROOT directory:
#    cp .env.example .env.local
#
# 2. Edit .env.local and fill in your API keys
#
# 3. Both frontend and backend will automatically read from this single file
#
# 4. Required variables:
#    - NEXT_PUBLIC_API_URL (frontend)
#    - PORT, CORS_ORIGIN (backend server)
#    - OPENAI_API_KEY or GOOGLE_API_KEY (at least one required)
#    - DEEPGRAM_API_KEY (backend speech recognition)
#
# 5. AI Model Selection:
#    - Models are selected in the app's setup screen, not in .env
#    - You can configure both OpenAI and Gemini keys
#    - Switch between models in the UI
#
# 6. Start development:
#    npm run dev
